function solve() {
    let counter = 1
    setInterval(() => {
        console.log(counter);
        counter++
    }, 1000)
}

// solve()

let counter = 1
function Timer() {
    console.log(counter);
    counter++
}

// setInterval(Timer, 1000)